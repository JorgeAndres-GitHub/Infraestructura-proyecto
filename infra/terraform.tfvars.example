# Archivo de ejemplo para variables de Terraform
# Copia este archivo a terraform.tfvars y ajusta los valores

location       = "eastus"
rg_name        = "rg-proyecto-demo"
prefix         = "jhdemo"  # Cambia por un prefijo único (máx 10 caracteres)
sql_admin_user = "sqladmin"
sql_admin_pass = "TuContraseñaSegura123!"  # Cambiar en producción
my_ip          = ""  # Tu IP pública para acceso al SQL, ej: "192.168.1.1"
